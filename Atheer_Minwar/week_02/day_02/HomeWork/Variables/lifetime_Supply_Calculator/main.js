var currentAge = 15;
var maxAge = 30;
var amountPerDay = 4;
var HumanlifeSpan = 79;
var consumption = (HumanlifeSpan * amountPerDay) * (maxAge - currentAge);

console.log("You will need " + consumption + " to last you until the ripe old age of " + HumanlifeSpan);
function square(num) {
    var square = num * num;
    
    console.log('The result of squaring the number ' + num + ' is ' + square);
    return square;
    }
    square(3);
//

function half(num) {
    var halfN = num / 2;
    console.log('Half of ' + num + ' is ' +  halfN);
    return halfN;
    }

    half(5);
var eventType = "click";

var myButton = document.querySelector("#half-button");


function myCallback(){
   
   document.getElementById("half-button");
   document.getElementById("percent1-input").value;
  
  document.getElementById("area-input").value;
  circle.document.getElementById("area-button");
}
myButton.addEventListener(eventType, myCallback);

function   circle(radius){


    circle.radius = radius;
}
    circle.area = function () {
        return Math.PI * this.radius * this.radius;
    }
function valus (mm){
    mm= number /2;
}